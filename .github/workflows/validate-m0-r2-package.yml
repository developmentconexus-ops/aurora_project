name: Validate M0 R2 package

on:
  push:
    branches:
      - docs/m0-r2-requirements
    paths:
      - scripts/validate_m0_r2_package.py
      - .github/workflows/validate-m0-r2-package.yml

permissions:
  contents: write

jobs:
  validate-r2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate R2 requirements package
        run: python scripts/validate_m0_r2_package.py
      - name: Generate documentation projections
        run: python scripts/generate_docs.py --output-root "$RUNNER_TEMP/aurora-generated"
      - name: Validate documentation system
        run: python scripts/validate_docs.py --generated-root "$RUNNER_TEMP/aurora-generated" --report "$RUNNER_TEMP/docs-validation-report.json"
      - name: Check generated projections
        run: python scripts/generate_docs.py --check
      - name: Remove one-shot validator after successful verification
        run: |
          rm -f scripts/validate_m0_r2_package.py .github/workflows/validate-m0-r2-package.yml
          git config user.name "developmentconexus-ops"
          git config user.email "development.conexus@gmail.com"
          git add -A
          git diff --cached --check
          git commit -m "chore: remove one-shot M0 R2 package validator"
          git push origin HEAD:docs/m0-r2-requirements
